# NeuraX Smart Cities Dataset v2 - Hidden Test Input

Use traffic_input.csv plus network.csv, nodes.csv, context.csv, roadworks.csv, signal_plans.csv, turn_restrictions.csv, planning_candidates.csv and od_demand_profiles.csv. evaluation_windows.csv identifies judge windows without revealing their hidden cause.

traffic_input.csv intentionally contains missing values, duplicates, spikes, stuck-sensor blocks, impossible negative readings and shuffled rows. Do not assume every anomaly is a real incident. Hidden causes and intervention reference outcomes are organizer-only.